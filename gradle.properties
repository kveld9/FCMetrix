# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Run tasks in parallel across decoupled modules (:app, :baselineprofile)
org.gradle.parallel=true

# Cache task outputs locally to accelerate repeated builds
org.gradle.caching=true

# Configuration Cache skips configuration phase on subsequent runs
org.gradle.configuration-cache=true

# Kotlin code style for this project
kotlin.code.style=official

# Required for KSP / Baseline Profile generated sources compatibility in AGP 9
android.disallowKotlinSourceSets=false
android.sync.suppressAgpWarnings=UNSUPPORTED_PROJECT_OPTION_USE

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
